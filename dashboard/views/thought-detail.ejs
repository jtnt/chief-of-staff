<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Chief of Staff</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="app">
    <%- include('partials/sidebar', { currentPage }) %>
    <main class="main">
      <div class="detail-header">
        <a href="/activity" class="back-link">&larr; Back to Activity</a>
      </div>

      <article class="detail-page">
        <header class="detail-meta">
          <h1 class="detail-title"><%= thought.title %></h1>
          <div class="detail-info">
            <% if (thought.date) { %>
              <span class="detail-date"><%= thought.date %></span>
            <% } %>
            <% if (thought.tags && thought.tags.length > 0) { %>
              <div class="detail-tags">
                <% thought.tags.forEach(tag => { %>
                  <span class="tag"><%= tag %></span>
                <% }) %>
              </div>
            <% } %>
          </div>
        </header>

        <div class="detail-content markdown-body">
          <%- thought.htmlContent %>
        </div>
      </article>
    </main>
  </div>
</body>
</html>
